  let newCandy = {
    candy: "Skittles",
    inStock: 200,
    weeklyAverage: 10,
  };
  function addCandyToInventory(inventory, newCandy){
     inventory.push(newCandy);
        for(let i = 0; i < inventory.lengt; i++){ 
      inventory += inventory[i].newCandy;
        };
    return inventory 
  };